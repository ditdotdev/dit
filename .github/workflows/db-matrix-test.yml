name: Database Matrix Tests

on:
  schedule:
    # Weekly on Sunday at 02:00 UTC (was nightly — #144). The matrix
    # tests run against Postgres + MySQL + SQLite containers; daily
    # cadence was over-spending vs the regression signal it produced.
    # Manual dispatch still available for ad-hoc runs.
    - cron: '0 2 * * 0'
  workflow_dispatch:
    # Allow manual trigger

permissions:
  contents: read

jobs:
  db-matrix:
    name: Test ${{ matrix.database }}
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        database:
    
        # Amazon DynamoDB
        # https://hub.docker.com/r/ditdotdev/dynamodb-local
        # Wrapped versions of amazon/dynamodb-local with VOLUME directives
        # Source Dockerfiles: tests/endtoend/db-matrix/dynamodb-local-*.Dockerfile
        - ditdotdev/dynamodb-local:latest
        - ditdotdev/dynamodb-local:3.3.0
        - ditdotdev/dynamodb-local:3.2.0
        - ditdotdev/dynamodb-local:3.1.0
        - ditdotdev/dynamodb-local:3.0.0
        - ditdotdev/dynamodb-local:2.6.1
        - ditdotdev/dynamodb-local:2.5.4

        # Apache Cassandra
        # https://hub.docker.com/_/cassandra
        - cassandra:5
        - cassandra:4
        - cassandra:4.0

        # Apache CouchDB
        # https://hub.docker.com/_/couchdb
        - couchdb:3.5
        - couchdb:3.4

        # ClickHouse
        # https://hub.docker.com/r/clickhouse/clickhouse-server
        - clickhouse/clickhouse-server:26.3
        - clickhouse/clickhouse-server:26.2
        - clickhouse/clickhouse-server:26.1

        # Couchbase Enterprise
        # https://hub.docker.com/_/couchbase
        - couchbase:enterprise-8.0.1
        - couchbase:enterprise-7.6.10
        - couchbase:enterprise-7.2.9

        # CockroachDB
        # https://hub.docker.com/r/cockroachdb/cockroach
        # https://hub.docker.com/r/ditdotdev/cockroach
        # Wrapped versions of cockroachdb/cockroach with VOLUME directives
        # Source Dockerfiles: tests/endtoend/db-matrix/cockroach-*.Dockerfile
        - ditdotdev/cockroach:v26.1.2
        - ditdotdev/cockroach:v25.4.8
        - ditdotdev/cockroach:v25.2.16
        - ditdotdev/cockroach:v24.3.30

        # Apache Hive
        # Not sure if this makes sense. 

        # Databricks
        # managed database service; no official DB server image on Docker Hub

        # Dragonfly
        # https://hub.docker.com/r/dragonflydb/dragonfly
        # Redis-compatible, multi-threaded in-memory datastore
        - docker.dragonflydb.io/dragonflydb/dragonfly:v1.38.0

        # Elasticsearch
        # https://hub.docker.com/r/ditdotdev/elasticsearch
        # Wrapped versions of elasticsearch with VOLUME directives
        # Source Dockerfiles: tests/endtoend/db-matrix/elasticsearch-*.Dockerfile
        - ditdotdev/elasticsearch:9.3.3
        - ditdotdev/elasticsearch:9.2.8
        - ditdotdev/elasticsearch:8.19.14

        # Google BigQuery
        # Managed cloud service; no official DB server image on Docker Hub

        # IBM Db2
        - icr.io/db2_community/db2:latest
        - icr.io/db2_community/db2:12.1.4.0
        - icr.io/db2_community/db2:12.1.3.0
        - icr.io/db2_community/db2:11.5.9.0
        - icr.io/db2_community/db2:11.5.8.0
        - icr.io/db2_community/db2:11.5.5.1

        # MariaDB
        # https://hub.docker.com/_/mariadb
        - mariadb:12
        - mariadb:11
        - mariadb:11.4
        - mariadb:10

        # Microsoft Access
        # Yeah, we aren't doing this one.

        # Microsoft Azure SQL DB
        # Cloud DB service; no official DB server image on Docker Hub

        # Microsoft SQL Server
        # https://hub.docker.com/r/ditdotdev/mssql-server
        # Wrapped versions of mcr.microsoft.com/mssql/server with VOLUME directives
        # Source Dockerfiles: tests/endtoend/db-matrix/mssql-*.Dockerfile
        - ditdotdev/mssql-server:2025
        - ditdotdev/mssql-server:2022
        - ditdotdev/mssql-server:2019
        - ditdotdev/mssql-server:2017

        # MongoDB
        - mongo:latest
        - mongo:8
        - mongo:8.2
        - mongo:8.0
        - mongo:7

        # MySQL
        - mysql:9
        - mysql:8

        # Neo4j
        # https://hub.docker.com/_/neo4j
        # Note: Neo4j skipped from 5.x to 2026.x (year-based versioning); there is no 2025 tag
        - neo4j:2026
        - neo4j:5

        # Oracle
        # https://hub.docker.com/r/ditdotdev/oracle-xe
        # https://hub.docker.com/r/ditdotdev/oracle-free
        # Wrapped versions of gvenzl/oracle-xe and gvenzl/oracle-free with VOLUME directives
        # Source Dockerfiles: tests/endtoend/db-matrix/oracle-*.Dockerfile
        #- ditdotdev/oracle-xe:latest
        #- ditdotdev/oracle-xe:latest-faststart
        #- ditdotdev/oracle-xe:slim
        - ditdotdev/oracle-xe:slim-faststart
        #- ditdotdev/oracle-xe:full
        #- ditdotdev/oracle-xe:full-faststart

        #- ditdotdev/oracle-xe:21
        #- ditdotdev/oracle-xe:21-faststart
        #- ditdotdev/oracle-xe:21-slim
        - ditdotdev/oracle-xe:21-slim-faststart
        #- ditdotdev/oracle-xe:21-full
        #- ditdotdev/oracle-xe:21-full-faststart

        #- ditdotdev/oracle-xe:18
        #- ditdotdev/oracle-xe:18-faststart
        #- ditdotdev/oracle-xe:18-slim
        - ditdotdev/oracle-xe:18-slim-faststart
        #- ditdotdev/oracle-xe:18-full
        #- ditdotdev/oracle-xe:18-full-faststart

        #- ditdotdev/oracle-xe:11
        #- ditdotdev/oracle-xe:11-faststart
        #- ditdotdev/oracle-xe:11-slim
        - ditdotdev/oracle-xe:11-slim-faststart
        #- ditdotdev/oracle-xe:11-full
        #- ditdotdev/oracle-xe:11-full-faststart

        # https://hub.docker.com/r/gvenzl/oracle-free
        #- ditdotdev/oracle-free:latest
        #- ditdotdev/oracle-free:latest-faststart
        #- ditdotdev/oracle-free:slim
        - ditdotdev/oracle-free:slim-faststart
        #- ditdotdev/oracle-free:full
        #- ditdotdev/oracle-free:full-faststart
        
        #- ditdotdev/oracle-free:23
        #- ditdotdev/oracle-free:23-faststart
        #- ditdotdev/oracle-free:23-slim
        - ditdotdev/oracle-free:23-slim-faststart
        #- ditdotdev/oracle-free:23-full
        #- ditdotdev/oracle-free:23-full-faststart

        #- ditdotdev/oracle-free:23.26.0
        #- ditdotdev/oracle-free:23.26.0-faststart
        #- ditdotdev/oracle-free:23.26.0-slim
        - ditdotdev/oracle-free:23.26.1-slim-faststart
        - ditdotdev/oracle-free:23.26.0-slim-faststart
        #- ditdotdev/oracle-free:23.26.0-full
        #- ditdotdev/oracle-free:23.26.0-full-faststart

        # OpenSearch
        # https://hub.docker.com/r/opensearchproject/opensearch
        # https://hub.docker.com/r/ditdotdev/opensearch
        # Wrapped versions of opensearchproject/opensearch with VOLUME directives
        # Source Dockerfiles: tests/endtoend/db-matrix/opensearch-*.Dockerfile
        - ditdotdev/opensearch:3.6.0
        - ditdotdev/opensearch:3.5.0
        - ditdotdev/opensearch:2.19.5

        # PostgreSQL
        - postgres:18
        - postgres:17
        - postgres:16
        - postgres:15
        - postgres:14

        # Qdrant
        # https://hub.docker.com/r/qdrant/qdrant
        # https://hub.docker.com/r/ditdotdev/qdrant
        # Wrapped versions of qdrant/qdrant with VOLUME directives
        # Source Dockerfiles: tests/endtoend/db-matrix/qdrant-*.Dockerfile
        - ditdotdev/qdrant:v1.17
        - ditdotdev/qdrant:v1.16

        # Redis
        # https://hub.docker.com/r/ditdotdev/redis
        # Wrapped versions of redis with VOLUME directives
        # Source Dockerfiles: tests/endtoend/db-matrix/redis-*.Dockerfile
        - ditdotdev/redis:8
        - ditdotdev/redis:7
        - ditdotdev/redis:7.2

        # Valkey
        # https://hub.docker.com/r/ditdotdev/valkey
        # Wrapped versions of valkey/valkey with VOLUME directives
        # Source Dockerfiles: tests/endtoend/db-matrix/valkey-*.Dockerfile
        - ditdotdev/valkey:9
        - ditdotdev/valkey:8
        - ditdotdev/valkey:7

        # ScyllaDB
        # https://hub.docker.com/r/scylladb/scylla
        # Cassandra-compatible high-performance database
        - scylladb/scylla:2026.1
        - scylladb/scylla:2025.4
        - scylladb/scylla:2025.3

        # SurrealDB
        # https://hub.docker.com/r/surrealdb/surrealdb
        # Multi-model database (document, graph, relational)
        - surrealdb/surrealdb:v3.0.5
        - surrealdb/surrealdb:v3

        # TiDB
        # https://hub.docker.com/r/pingcap/tidb
        # https://hub.docker.com/r/ditdotdev/tidb
        # Wrapped versions of pingcap/tidb with VOLUME directives
        # Source Dockerfiles: tests/endtoend/db-matrix/tidb-*.Dockerfile
        - ditdotdev/tidb:v8.5.6
        - ditdotdev/tidb:v8.1.2

        # TigerGraph
        # https://hub.docker.com/r/tigergraph/tigergraph
        # https://hub.docker.com/r/ditdotdev/tigergraph
        # Wrapped versions of tigergraph/tigergraph with VOLUME directives
        # Source Dockerfiles: tests/endtoend/db-matrix/tigergraph-*.Dockerfile
        # Note: previously we were using TigerGraph Enterprise Edition. The need to get updated license keys was causing problems.
        # Moving to CE because it has the same functionality needed for dit. Keeping the old Enterprise images around for now in case we need to revert.
        # - ditdotdev/tigergraph:latest
        # - ditdotdev/tigergraph:4.2.2
        - ditdotdev/tigergraph-ce:4.2.2
        # - ditdotdev/tigergraph:4.1.4

        # TimescaleDB
        # https://hub.docker.com/r/timescale/timescaledb
        # PostgreSQL extension for time-series data
        - timescale/timescaledb:2.26.3-pg18
        - timescale/timescaledb:2.26.3-pg17
        - timescale/timescaledb:2.26.3-pg16

        # YugabyteDB
        # https://hub.docker.com/r/yugabytedb/yugabyte
        # PostgreSQL-compatible distributed SQL database
        - yugabytedb/yugabyte:2025.2.2.2-b11
        - yugabytedb/yugabyte:2025.1.3.2-b1

        # Apache Kafka (KRaft mode - standalone, no Zookeeper required)
        # https://hub.docker.com/r/apache/kafka
        # Event streaming platform for cloud-native microservices
        - apache/kafka:4.0.2

        # Apache Solr
        # https://hub.docker.com/_/solr
        # Enterprise search platform
        - solr:9.10.1

        # ArangoDB
        # https://hub.docker.com/_/arangodb
        # Multi-model database (document, graph, key-value)
        - arangodb:3.12.9

        # Chroma
        # https://hub.docker.com/r/chromadb/chroma
        # https://hub.docker.com/r/ditdotdev/chroma
        # Wrapped version of chromadb/chroma with VOLUME directive
        # Source Dockerfile: tests/endtoend/db-matrix/chroma-*.Dockerfile
        # AI-native vector database for embeddings and RAG
        - ditdotdev/chroma:1.5.7

        # CrateDB
        # https://hub.docker.com/r/crate/crate
        # Distributed SQL database for IoT and machine data
        - crate/crate:6.3.1

        # InfluxDB
        # https://hub.docker.com/_/influxdb
        # https://hub.docker.com/r/ditdotdev/influxdb
        # Wrapped version of influxdb with VOLUME directive
        # Source Dockerfile: tests/endtoend/db-matrix/influxdb-*.Dockerfile
        # Time-series database for monitoring and observability
        - ditdotdev/influxdb:3.9.1-core

        # Meilisearch
        # https://hub.docker.com/r/getmeili/meilisearch
        # https://hub.docker.com/r/ditdotdev/meilisearch
        # Wrapped version of getmeili/meilisearch with VOLUME directive
        # Source Dockerfile: tests/endtoend/db-matrix/meilisearch-*.Dockerfile
        # Fast, typo-tolerant search engine
        - ditdotdev/meilisearch:v1.42.1

        # MinIO
        # https://hub.docker.com/r/minio/minio
        # S3-compatible object storage for cloud-native applications
        - minio/minio:latest

        # NATS with JetStream
        # https://hub.docker.com/_/nats
        # https://hub.docker.com/r/ditdotdev/nats
        # Wrapped version of nats with VOLUME directive
        # Source Dockerfile: tests/endtoend/db-matrix/nats-*.Dockerfile
        # Cloud-native messaging system with persistence
        - ditdotdev/nats:2.12.7

        # QuestDB
        # https://hub.docker.com/r/questdb/questdb
        # https://hub.docker.com/r/ditdotdev/questdb
        # Wrapped version of questdb/questdb with VOLUME directive
        # Source Dockerfile: tests/endtoend/db-matrix/questdb-*.Dockerfile
        # High-performance time-series database
        - ditdotdev/questdb:9.3.5

        # RabbitMQ
        # https://hub.docker.com/_/rabbitmq
        # Message broker for distributed systems
        - rabbitmq:4.0.9

        # StarRocks
        # https://hub.docker.com/r/starrocks/allin1-ubuntu
        # https://hub.docker.com/r/ditdotdev/starrocks
        # Wrapped version of starrocks/allin1-ubuntu with VOLUME directive
        # Source Dockerfile: tests/endtoend/db-matrix/starrocks-*.Dockerfile
        # High-performance OLAP database
        - ditdotdev/starrocks:4.1.0

        # Typesense
        # https://hub.docker.com/r/typesense/typesense
        # https://hub.docker.com/r/ditdotdev/typesense
        # Wrapped version of typesense/typesense with VOLUME directive
        # Source Dockerfile: tests/endtoend/db-matrix/typesense-*.Dockerfile
        # Open-source search engine alternative to Elasticsearch
        - ditdotdev/typesense:30.2

        # Weaviate
        # https://hub.docker.com/r/semitechnologies/weaviate
        # https://hub.docker.com/r/ditdotdev/weaviate
        # Wrapped version of semitechnologies/weaviate with VOLUME directive
        # Source Dockerfile: tests/endtoend/db-matrix/weaviate-*.Dockerfile
        # AI-native vector database for semantic search
        - ditdotdev/weaviate:1.37.0

        # Snowflake
        # Snowflake is a managed cloud service; no official DB server image on Docker Hub (only third-party/local solutions and Snowflake’s own OCI registry for app images).

        # Splunk
        # Not a database server

        # DuckDB
        # Same as SQLite — embedded/in-process database with no server container to version.

        # SQLite
        # SQLite is fundamentally incompatible with dit's model of versioning database servers with persistent storage.

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.GO_MODULES_TOKEN }}

      - name: Configure Git for private modules
        run: |
          git config --global url."https://x-access-token:${{ secrets.GO_MODULES_TOKEN }}@github.com/".insteadOf "https://github.com/"
          git config --global credential.helper store
          echo "https://x-access-token:${{ secrets.GO_MODULES_TOKEN }}@github.com" > ~/.git-credentials
        shell: bash

      - name: Configure Git user
        run: |
          git config --global user.name "test.runner"
          git config --global user.email "test@runner.com"

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.2'

      - name: Configure Go for private repos
        run: |
          go env -w GOPRIVATE=github.com/ditdotdev/*
          go env -w GONOPROXY=github.com/ditdotdev/*
          go env -w GONOSUMDB=github.com/ditdotdev/*
          go env -w GOPROXY=https://proxy.golang.org,direct
        shell: bash
        env:
          GOPRIVATE: github.com/ditdotdev/*
          GONOPROXY: github.com/ditdotdev/*
          GONOSUMDB: github.com/ditdotdev/*

      - name: Output Kernel Info
        run: |
          docker pull busybox
          docker run busybox uname -a

      - name: Build Dit from source
        run: |
          VERSION=$(gh release view --json tagName -q .tagName)
          echo "Building with version: $VERSION"
          make build VERSION=$VERSION
          build/dit --version
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Install OpenZFS
        run: |
          echo "Installing ZFS for Ubuntu $(lsb_release -rs) kernel $(uname -r)..."
          sudo apt-get update
          
          echo "Installing ZFS userspace tools and kernel modules..."
          sudo apt-get install -y zfsutils-linux
          
          echo "Loading ZFS kernel modules..."
          sudo modprobe zfs
          
          echo "Verifying ZFS installation..."
          zfs version
          zpool version
          lsmod | grep zfs
          
          echo "Testing ZFS functionality..."
          # Create a test file for ZFS pool
          sudo dd if=/dev/zero of=/tmp/zfs-test-file bs=1M count=100
          sudo zpool create testpool /tmp/zfs-test-file
          sudo zfs create testpool/testfs
          sudo zfs list
          sudo zpool destroy testpool
          sudo rm -f /tmp/zfs-test-file
          echo "ZFS installation and test completed successfully!"
      
      - name: Free up disk space
        run: |
          echo "Before cleanup:"
          df -h
          # Remove unnecessary preinstalled software to make room for database images
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"
          echo "After cleanup:"
          df -h
      
      - name: Setup Test Requirements
        run: |
          # Install BATS test framework
          sudo npm install -g bats
          bats --version
          
          # Copy build/dit to dit
          cp build/dit dit
      
      # Authenticate to Docker Hub so 'dit install' can pull ditdotdev/* images
      # without hitting the anonymous pull rate-limit on shared runner IPs (#167).
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Install Dit infrastructure
        run: |
          export PATH=$GITHUB_WORKSPACE/build:$PATH
          build/dit install --verbose

      - name: Wait for Dit server to be ready
        run: |
          echo "Waiting for dit-docker-server container to start..."
          timeout 120 bash -c 'until docker inspect dit-docker-server --format "{{.State.Running}}" 2>/dev/null | grep -q true; do sleep 1; done'
          echo "Container is up, waiting for Java server on port 5001..."
          timeout 120 bash -c 'until docker exec dit-docker-server nc -z localhost 5001 2>/dev/null; do sleep 1; done'
          echo "Dit server is ready"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2
      
      - name: Run database matrix test for ${{ matrix.database }}
        run: |
          export PATH=$GITHUB_WORKSPACE/build:$PATH
          echo "Testing database: $DATABASE_VERSION"
          bats tests/endtoend/db-matrix/db-matrix.bats
        env:
          DATABASE_VERSION: ${{ matrix.database }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-west-2
          # Unique S3 namespace per run to avoid collisions. See issue #118.
          E2E_RUN_SUFFIX: run-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Cleanup database-specific S3 prefix
        if: always()
        run: |
          # Compute the same S3 path that db-matrix.bats uses for this database
          DB_VERSION="${{ matrix.database }}"
          if [[ "$DB_VERSION" == *"/"* ]]; then
            DB="${DB_VERSION##*/}"
            DB="${DB%%:*}"
          else
            DB="${DB_VERSION%%:*}"
          fi
          VERSION="${DB_VERSION#*:}"
          SAFE_VERSION="${VERSION//\//-}"
          SAFE_VERSION="${SAFE_VERSION//:/-}"
          S3_PATH="s3://dit-testdata/e2etest/run-${{ github.run_id }}-${{ github.run_attempt }}/${DB}-${SAFE_VERSION}"
          echo "Cleaning up S3 path: $S3_PATH"
          aws s3 rm "$S3_PATH" --recursive || true
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-west-2

      - name: Cleanup on failure
        if: failure()
        run: |
          export PATH=$GITHUB_WORKSPACE/build:$PATH
          build/dit uninstall --force || true
      
      - name: Cleanup Docker resources
        if: always()
        run: |
          echo "Cleaning up Docker images and containers..."
          docker system prune -af --volumes
          df -h
