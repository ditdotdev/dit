tests:
  - "remove $REMOTE assets":
      command: aws s3 rm $URI --recursive
      env:
        get:
          - URI
          - REMOTE
  - "remove $REPO":
      command: d3 rm -f $REPO
      env:
        get:
          - REPO