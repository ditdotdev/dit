## d3 remote ls

List remotes for a repository

```
d3 remote ls [REPOSITORY] [flags]
```

### Options

```
  -r, --remote string   name of the remote provider, defaults to origin (default "origin")
  -t, --tags strings    tag to filter latest commit, if commit is not specified
  -h, --help            help for ls
```

### Options inherited from parent commands

```
      --context string   Datadatdat Provider Context
```

### SEE ALSO

* [d3 remote](d3_remote.md)	 - Add, log, ls and rm remotes

